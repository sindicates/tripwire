# Use your machine's local network IP (not localhost) when testing on a physical device with Expo Go.
# Find it with: ipconfig getifaddr en0  (macOS) or ip addr show (Linux)
EXPO_PUBLIC_API_URL=http://localhost:8000
